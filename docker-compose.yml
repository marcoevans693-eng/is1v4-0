version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: intellisys1-qdrant
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
    external: true
    name: qdrant_data
